export function Footer() {
  return (
    <footer className="mt-20 border-t border-border/70 py-8">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-4 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between sm:px-6">
        <p className="font-medium text-foreground">FraudGuard AI</p>
        <p>Your AI-powered second brain against voice fraud.</p>
        <p className="text-xs">Prototype — demo data only.</p>
      </div>
    </footer>
  );
}
